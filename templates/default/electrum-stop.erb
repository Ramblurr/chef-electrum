#!/bin/bash

[ `whoami` == "<%= node['electrum']['electrum_user'] %>" ] || exit 1

PYTHON=/usr/bin/python
CWD=<%= node['electrum']['prefix'] %>/src/electrum
SERVER=server.py

cd $CWD
$PYTHON -u $SERVER stop
